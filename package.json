{
  "name": "n-queens-helper-ui",
  "version": "0.3.2",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "surge build n-queens-helper.surge.sh",
    "preversion": "npm run build",
    "postversion": "npm run deploy && git push && git push origin --tags"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "resolutions": {
    "lodash": "4.17.14"
  }
}
